<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.this_is_ayan.findmyadvocate" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".ParseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar" >
        <activity
            android:name=".Activities.SplashActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.MainActivity" />
        <activity
            android:name=".Activities.PostCase"
            android:label="@string/title_activity_post_case" >
        </activity>

        <activity
            android:name=".Activities.LogInSignUp"
            android:label="@string/title_activity_log_in_sign_up" >
        </activity>

        <activity
            android:name=".Activities.HomeActivity"
            android:label="Home Activity" >
        </activity>

      <!--  <activity
            android:name=".Activities.SplashActivity"
            android:label="Splash Activity Activity" >
        </activity>-->
    </application>

</manifest>
